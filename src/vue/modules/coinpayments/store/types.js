const mutations = {
  SET_DEPOSIT_DETAILS: 'SET_DEPOSIT_DETAILS',
}

const actions = {
  LOAD_DEPOSIT: 'LOAD_DEPOSIT',
  LOAD_PENDING_ISSUANCES: 'LOAD_PENDING_ISSUANCES',
}

const getters = {
}

export const types = {
  ...mutations,
  ...actions,
  ...getters,
}
